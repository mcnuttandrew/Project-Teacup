<div class="row">
	<div class="col-xs-9"> 
	<h5> 	
		<%= comment.get('content')%>
		<br> 
		<strong >Posted By 
		<a href="#/users/<%=comment.get('user_id')%>"><%= comment.get('username')%> </a>
		</strong>
	</h5>
	</div>
	<div class="col-xs-2" style="vertical-align:bottom"> 
		<%if(comment.get('user_id') === $("#currentUser").data().id) {%>
			<button class="btn cancel-button btn-mini" id="remove-comment" style="background:rgba(0,0,0,0)"> X </button>
		<%} %>
	</div>
</div>